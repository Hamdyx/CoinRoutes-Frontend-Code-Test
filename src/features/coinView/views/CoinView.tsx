import { Flex } from 'antd';

import CoinsDropdown from '../components/CoinsDropdown';

function CoinView() {
  return (
    <Flex>
      <CoinsDropdown />
    </Flex>
  );
}

export default CoinView;
